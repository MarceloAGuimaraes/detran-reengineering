<div class="row">
  <div class="col-md-8">
    <div class="card">
      <div class="card-header card-header-primary">
        <h4 class="card-title">Consulta Emissão de CNH</h4>
        <p class="card-category">Insira as informações</p>
      </div>
      <div class="card-body">
        <%= simple_form_for :drivers, url: drivers_track_request_path, method: :get, remote: true do |f| %>
          <div class="row">
            <div class="col-md-12">
              <%= f.input :service_type, label: false, as: :select, collection: Detran.services_types, label_method: :second, value_method: :first, input_html: { class: 'selectpicker' } %>
            </div>
          </div>
          <div class="row">
            <div class="col-md-5">
              <div class="form-group bmd-form-group">
                <label class="bmd-label-floating">Data de Nascimento</label>
                <%= f.input :birth_date, label: false, as: :string, input_html: { class: 'form-control datepicker'} %>
              </div>
            </div>
            <div class="col-md-5">
              <div class="form-group bmd-form-group">
                <label class="bmd-label-floating">CPF</label>
                <%= f.input :cpf, label: false, as: :string %>
              </div>
            </div>
          </div>
          <%= f.submit "Pesquisar", class: "btn btn-primary pull-right" %>
        <% end %>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="card card-profile">
      <div class="card-avatar">
        <a href="#detran-mg">
          <%= image_tag(ActionController::Base.helpers.asset_path("detran-mg.jpg")) %>
        </a>
      </div>
      <div class="card-body">
        <h6 class="card-category">Documento: Carteira Nacional de Habilitação (CNH).</h6>
        <h4 class="card-title">Descrição</h4>
        <p class="card-description">
        Acompanhe aqui os serviços solicitados referentes à Carteira Nacional de Habilitação (CNH).
        No formulário, selecione o serviço que foi solicitado e preencha o número de CPF e data de nascimento;
        Clique em "pesquisar" para visualizar o resultado.
        </p>
      </div>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-md-12" id="track-request-result">
  </div>
</div>